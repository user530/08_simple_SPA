import { MainProductsWrapper } from '../assets/styled/MainProducts';

const MainProducts: React.FC = () => {
  return <MainProductsWrapper>Products</MainProductsWrapper>;
};

export default MainProducts;
