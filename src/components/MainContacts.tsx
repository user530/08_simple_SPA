import { MainContactsWrapper } from '../assets/styled/MainContacts';

const MainContacts: React.FC = () => {
  return <MainContactsWrapper>Contacts</MainContactsWrapper>;
};

export default MainContacts;
